streamlit>=1.30.0
folium>=0.15.0
streamlit-folium>=0.22.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# AI 서비스 레이어 (khuda-data/KHUBA_OB_Project 의 src.* 연동용)
# xgboost/shap은 버전에 따라 같은 모델 파일(final_model.pkl)에서도 예측값이 달라질 수 있어
# 정확한 버전으로 고정한다(local/배포 환경이 서로 다른 버전을 설치해서 예측값이 어긋나는 것을 방지).
# 로컬 개발 환경에서 전체 페이지가 정상 동작하는 것을 확인한 버전.
joblib>=1.3
xgboost==2.1.4
shap==0.49.1
