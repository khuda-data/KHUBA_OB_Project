streamlit>=1.30.0
folium>=0.15.0
streamlit-folium>=0.22.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# AI 서비스 레이어 (khuda-data/KHUBA_OB_Project 의 src.* 연동용)
# xgboost는 버전에 따라 같은 모델 파일(final_model.pkl)에서도 예측값이 달라질 수 있어
# 로컬에서 전체 페이지가 정상 동작하는 것을 확인한 버전으로 고정한다.
# shap은 고정하지 않는다 — 0.49.1로 고정해봤더니 배포 서버(Python 3.14)에서 shap의
# 컴파일된 확장 모듈(_cext)에 대한 3.14용 바이너리 휠이 없어 ImportError로 앱이 죽었다.
# pip가 각 환경에 맞는 휠을 알아서 고르도록 범위로 둔다.
joblib>=1.3
xgboost==2.1.4
shap>=0.45
